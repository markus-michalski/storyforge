pytest
# Floor 1.3: first release supporting pytest 9.x (1.0-1.2 pin pytest<9),
# which is what this repo runs.
pytest-asyncio>=1.4.0,<2.0
# Upper bound: ruff 0.16.0 flags 332 new findings (DTZ/SIM rules) on
# unrelated existing code, breaking CI. Investigate and fix separately,
# then remove this cap.
ruff<0.17.0
mypy
types-PyYAML
