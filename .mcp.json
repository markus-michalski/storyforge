{
  "mcpServers": {
    "storyforge-mcp": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/run-server",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/storyforge-server/run.py"]
    }
  }
}
